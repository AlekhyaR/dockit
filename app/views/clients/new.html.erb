<h2>New Client</h2>
<%= form_for @client do |f| %>
<table>
	<tr><td>Name:</td>
		<td><%= f.text_field :name %></td>
	</tr>
	<%= render :partial => 'addr_fields', :locals => {:f => f} %>
	<tr>
		<td><%= f.submit :submit %></td>
	</tr>
</table>
<% end %>
